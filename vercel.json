{
  "routes": [
    { "src": "/js/(.*)", "dest": "/js/$1" },
    { "src": "/templates/(.*)", "dest": "/hardware.html" }
  ],
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python"
    }
  ]
}
